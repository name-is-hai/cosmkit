#!/usr/bin/env bash
set -euo pipefail

COSMKIT_HOME="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

source "$COSMKIT_HOME/scripts/lib/common.sh"
source "$COSMKIT_HOME/scripts/steps/layer.sh"

case "${1:-host}" in
  host)
    layer_host_packages
    ;;
  input)
    layer_input_packages
    ;;
  nvidia)
    layer_nvidia_packages
    ;;
  all)
    layer_host_packages
    layer_input_packages
    ;;
  full)
    layer_host_packages
    layer_input_packages
    layer_nvidia_packages
    ;;
  *)
    die "Usage: cosmkit-layer [host|input|nvidia|all|full]"
    ;;
esac
